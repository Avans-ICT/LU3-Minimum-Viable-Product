# App
APP_ENV=development
APP_NAME=AI Recommender MVP

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# CORS (dev: * is ok, later: http://localhost:5173, etc.)
CORS_ORIGINS=*

# ML
MODEL_ARTIFACT_PATH=ml/artifacts/current
MODEL_VERSION=baseline
